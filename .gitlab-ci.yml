stages: 
  - build
  - deploy

image: node:12.13.0-alpine

build:
  stage: build
  script:
    - echo CI_COMMIT_TAG
deploy:
  stage: deploy
  script:
    - npm i -g firebase-tools
    - firebase deploy --token $FIREBASE_TOKEN
  only:
    - tags
